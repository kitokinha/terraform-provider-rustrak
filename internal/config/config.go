package config

const (
	// Version of the provider
	Version = "0.1.0"

	// UserAgent is the user agent for the provider
	UserAgent = "terraform-provider-rustrak/" + Version

	// DefaultHostURL is the default host for rustrak API
	DefaultHostURL = "https://api.rustrak.dev"
)
